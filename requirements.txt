# Web 层
flask>=3.0
PyJWT>=2.8                       # 多端 token 登录（services/token_auth.py）

# LangGraph 多智能体编排
langgraph>=1.0
langchain>=0.3                   # langfuse.langchain 集成依赖完整 langchain 包
langchain-openai>=0.2
langchain-core>=0.3
langgraph-checkpoint-sqlite>=3.0  # 本地兜底路径的对话持久化

# LLM 兼容接口 / 天气 / LangGraph 通信
requests>=2.31

# LLM 轨迹观测（自建 Langfuse，可选启用）
langfuse>=3.0

# Prometheus /metrics 指标（验收 1.5 看板，可选启用）
prometheus-client>=0.20

# 生产 WSGI（Docker 镜像默认入口）
waitress>=3.0

# LangGraph 本地 dev 服务器（容器内 langgraph dev）
langgraph-cli[inmem]>=0.1

# 环境变量
python-dotenv>=1.0
