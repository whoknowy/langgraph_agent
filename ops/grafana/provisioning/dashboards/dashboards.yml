# Grafana 看板自动加载（ops/grafana/dashboards/ 下所有 json）
apiVersion: 1

providers:
  - name: flight-agent
    orgId: 1
    folder: 航空客服
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
